/** Hot audit log retention before archive (days). */
export const AUDIT_RETENTION_DAYS = 90;

/** Batch size for archive $merge / delete cycles (M0-friendly). */
export const AUDIT_ARCHIVE_BATCH_SIZE = 500;
